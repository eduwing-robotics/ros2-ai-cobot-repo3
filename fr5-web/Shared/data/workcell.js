// 작업셀 실측 — **검증된 실세계 값의 단일 출처.**
//
// 규칙: **값 하나마다 출처·날짜·방법을 붙인다.** 출처 없는 숫자는 여기 못 들어온다.
// 다시 재는 수고를 없애려고 만든 파일이므로, 어떻게 얻었는지가 값만큼 중요하다.
//
// 기준 좌표계는 **로봇 베이스**다. 게이트(`config.yaml workspace`)는 **사용자 좌표계 1**
// 기준이라 그대로 쓰면 안 된다 — `Shared/view3d/workspace.js` 의 `toBase()` 가 환산한다.
//
// ── 2026-08-07 에 하루를 잃은 이유 (다시는)
// `/state.tcpMmDeg` 가 베이스 기준인 줄 알았는데 **user1 기준**이었다. user1 이
// `(−401.8, +497.3, +342.1)` 만큼 떨어져 있어서, 로봇이 자기 밑에 앉은 판을
// `(305, −516)` 으로 보고했다. 그걸 "받침이 172mm 있나 보다" 로 메우려다 화면에
// 없는 상자를 만들었다. **숫자가 안 맞으면 형상을 지어내지 말고 좌표계를 의심한다.**
// 전말: `docs/evidence/2026-08-07/workcell-remeasure.md`

/** 값마다 붙는 출처. `method` 가 곧 신뢰도다. */
const src = (label, method, date = '2026-08-07') => ({ label, method, date });

/**
 * 로봇이 올라앉은 이동식 카트.
 *
 * ── §게이트와 다른 점 (2026-08-08 줄자 재측정 뒤)
 * `wMm/dMm` 은 **실물 겉치수**(줄자)이고, 게이트가 지키는 범위는 `config.yaml` 의
 * `카트 상판` 상자 **858 × 587.3** 이다. 둘은 성격이 달라서 **일부러 안 맞춘다** —
 * 게이트는 "못 가는 데를 막아도 손해가 없다" 는 쪽으로 잡은 판정 부피다.
 *
 *   가로  실물 808  vs 게이트 858  → 게이트가 **50mm 넓다.** 빈 공기를 막는다 (안전)
 *   세로  실물 598  vs 게이트 587.3 → 게이트가 **10.7mm 좁다.** ⚠ 상판 테두리
 *         **편측 5.35mm 가 판정 밖**이다. 로봇이 거기까지 못 짚어서 생긴 차다.
 *
 * ⚠ **세로 쪽은 보수적이지 않다.** 넓히면 안전하지만 그때 거치대 자리도 같이 밀린다
 * (거치대는 카트 앞면에 붙어 있다) — 그래서 손대지 않고 실기 담당자 판단을 기다린다.
 */
export const CART = {
  // 줄자 (실기 담당자 · 2026-08-08 재측정). 이 값을 **셋이 서로 다르게 적고 있었다** —
  // 여기 810×597 · 런북 858×580 · 게이트 858×587.3. 재서 닫았고, 여기와 런북을 맞췄다.
  // 재측정이 옛 810×597 과 2·1mm 차라 **여기 값이 사실상 맞았고 런북 쪽이 틀렸다** —
  // 런북의 858 은 줄자값이 아니라 게이트 상자(외삽 포함)를 옮겨 적은 것으로 보인다.
  // ⚠ **게이트 상자는 안 고쳤다.** 성격이 다르다 — 아래 §게이트와 다른 점.
  wMm: 808, dMm: 598,          // 줄자 (실기 담당자 · 2026-08-08)
  heightMm: 1000,              // 줄자 985 + 상판 15 — 바닥에서 데크 윗면까지
  offsetMm: [0, 0],            // 게이트 상판 중심이 (+11, −7) 이라 사실상 0 이다
  facingRad: 0,                // ⚠ 미측정 — 문이 어느 면인지
};

/**
 * 카트 문면 앞에 선 **이동식 승강 거치대** — 흰 각파이프 + 백색 상판 + 하단 선반 + 캐스터 4.
 * 영상: `KakaoTalk_Video_2026-08-08-13-25-59.mp4` (실기 담당자).
 *
 * ⛔ **가로·세로·높이·자리를 여기 안 적는다.** 게이트 상자 `작업대` 가 정본이다
 * (`FR5/bridge/config.yaml` · 손목 뎁스 1534점 · 상판 평면 잔차 2.9mm · 2026-08-08).
 * 베껴 두면 안 되는 이유가 그 파일에 이미 적혀 있다 — *"이 작업대는 바뀔 예정이다(실기 담당자) —
 * 바뀌면 이 세 줄만 다시 잰다."* 베낀 순간 다시 재는 날 그림과 게이트가 조용히 갈라진다.
 * 그래서 `workspace.js` 가 상자에서 읽어 그리고, **게이트가 모르는 동안은 안 그린다.**
 *
 * ✅ **그 상자의 자리는 2026-08-08 에 고쳤다** — 옛 값은 카트를 738×196mm 파고들었고,
 * 이 거치대를 3D 에 세우는 순간 드러났다. 실기 담당자 실물 확인 *"카트에 딱 붙어 중앙정렬"* 로
 * 닫혔다. 크기(795×453)는 실측 그대로고 자리만 카트 상자에서 유도했다.
 * **아무도 검증한 적 없던 값이었다** — 카트는 교차검증이 셋인데 작업대만 evidence 가 없었다.
 * 전말: `docs/evidence/2026-08-08/stand-vs-cart-overlap.md`
 *
 * ⚠ 그 상자가 담지 **않는** 것 둘 — 게이트는 상판만 알면 되기 때문이다:
 *   · 높이 — 게이트가 쓰는 888mm 는 **뎁스**값이고 줄자는 850 이다. 상판까지 쟀는지 프레임까지
 *     쟀는지가 갈렸고, 게이트는 높은 쪽을 쓴다(낮게 잡으면 손끝이 파고든다). 그림도 같이 쓴다
 *   · 기울기 2.02° — 그림은 수평으로 그린다. 게이트가 `marginMm: 20` 으로 이미 덮는다
 *
 * 아래는 **상판 밑 형태**뿐이다. 판정에 안 쓰이므로 영상 관찰로 족하다.
 */
export const SIDE_STAND = {
  shelfHMm: 200,               // ⚠ 영상 관찰 — 하단 선반 윗면 (바닥 기준)
  tubeMm: 25,                  // ⚠ 영상 관찰 — 각파이프 한 변
  casterMm: 50,                // ⚠ 영상 관찰
  measured: false,             // ← 상판 밑을 자로 대면 true. 상판은 게이트가 이미 실측이다
};

/** 로봇이 볼트로 앉은 판. 이 판 윗면이 곧 로봇 장착면이다. */
export const MOUNT_PLATE = {
  sideMm: 229,                 // 줄자 (실기 담당자)
  thickMm: 12.8,               // 뎁스 2점 차 (−335.7 − (−348.5))
  topZMm: 6.4,                 // 뎁스 + user1 환산 · 베이스 기준
};

/**
 * URDF `base_link` 원점을 베이스 좌표 어디에 둘지.
 *
 * **0 이다** — 실측으로 확인했다. FAIRINO 컨트롤러 원점과 URDF 장착면이 같은 높이다
 * (마운트판 윗면 +6.4mm · 측정 오차 안). 한때 −152·−186·−345.8 을 넣어 봤지만 전부
 * 좌표계를 잘못 읽은 결과였다. **여기에 값을 넣고 싶어지면 좌표계부터 의심한다.**
 */
export const URDF_BASE_Z_MM = 0;

/**
 * 바닥. 게이트가 주는 상판 z 에서 카트 높이를 빼서 얻는다 — **상수로 안 적는다.**
 * 상판이 움직이면 바닥도 같이 움직여야 하는데 따로 적으면 갈라진다.
 *
 * 교차검증 — 뎁스로 바닥을 직접 재면 베이스 기준 **−981mm**, 여기 계산은 **−1004mm**.
 * 23mm 차 (1m 재기 + 렌즈 오프셋 80mm 포함).
 */
export const floorZMm = (tableTopZMmBase) => tableTopZMmBase - CART.heightMm;

/**
 * 터틀봇(AMR) **홈** — 조립 시나리오가 항상 여기서 시작한다 (실기 담당자 2026-08-28).
 * 홈이 곧 브링업 자리이므로 **odom 원점이 여기**다. `frame: "table"` 경로의 기준점이고,
 * 계약이 말하는 `tableToOdom` 이 이 값이다 (`TB-CONTRACT.md` §경로).
 *
 * ⛔ **좌표계 = 사용자 좌표계 1 이다. 베이스가 아니다.**
 * 판 값을 `config.yaml workspace` 에서 가져왔고 **그게 user1 기준**이기 때문이다
 * (이 파일 머리말 §게이트는 user1 기준). 그리는 쪽은 반드시
 * `Shared/data/frames.js` 의 `toFrame()` 을 거친다 (`docs/ref/contract/FRAMES.md`).
 *
 * ⛔ **2026-08-28 에 여기서 다쳤다.** 이 값을 베이스로 착각해 그대로 씬에 박았더니
 * 터틀봇이 **판 밑 342.1mm 허공**에 떴다 (= user1 의 z 오프셋 그대로). 판정면은 변환을
 * 거쳐 제자리였는데 터틀봇만 틀려서, 화면만 보면 「높이 상수가 틀렸나」로 읽혔다.
 * **머리말이 경고한 그 함정의 네 번째 사례다** — 숫자가 안 맞으면 좌표계부터 의심한다.
 *
 * ── 어떻게 얻었나 (2026-08-28 · 줄자 · 실기 담당자)
 * 잰 것은 **판 가장자리에서 로봇까지의 거리 둘**이고, 베이스 좌표는 거기서 **계산**했다 —
 * 판 자체는 로봇이 짚어 이미 실측돼 있다(`config.yaml` §작업영역 · 2026-08-18).
 *
 *   작업대1 **오른쪽 변**(x **1088.1** · **벽「옆 판」에 닿는 면**)에서 바퀴 센터까지 → 255mm
 *   작업대1 **카트 방향 가로변**(y −798.2 · 카트 앞면에 밀착)에서 터틀봇 센터까지 → 150mm
 *
 *   x = 1088.1 − 255 = 833.1     y = −798.2 − 150 = −948.2      z = 상판 −380.9
 *
 * ⛔ **「옆 판」은 벽 이름이지 작업대2 가 아니다** (2026-08-28 에 여기서 또 틀렸다).
 * `config.yaml` 의 작업대1 주석 *"오른쪽 면이 옆 판에 닿는다"* 를 「옆에 있는 판 = 작업대2」로
 * 읽어 **반대편 변(288.1)에서 쟀고**, 터틀봇이 290mm 왼쪽에 섰다. `walls:` 에 `- name: 옆 판`
 * 이 있고 그 선분이 `a[1088.1, −995.3]` 이다 — **이름이 겹치면 `walls:` 를 먼저 본다.**
 *
 * ── 검산
 * 버거 발자국(178 × 138)을 얹으면 네 변 여유가 **벽쪽 166 · 작업대2 쪽 456 · 카트쪽 81 ·
 * 낭떠러지쪽 281mm** 로 전부 판 안이다. 벽쪽 166mm 는 「벽을 등지고 주차」와 맞는 값이다.
 *
 * ── 요각 (2026-08-28 · 실기 담당자 확인)
 * **벽을 등지고 주차한다 — 정면이 작업대2 쪽**이다. user1 은 회전 0 이라 베이스와 축이 같고,
 * 벽은 +x 쪽(x 1088.1)이므로 정면은 −x = **180°**. 두 진술이 서로 맞물린다(등 ↔ 정면).
 * ⚠ **각도기로 잰 값은 아니다** — 「등지고 선다」는 운용 사실에서 나온 값이라 ±몇 도는 모른다.
 * 홈 복귀 정밀도가 문제되면 그때 실측한다.
 * ⚠ **작업대1 을 굴리면 이 값이 조용히 틀린다** — 판에 캐스터가 달려 있다(`config.yaml` §거치대2
 *   와 같은 경고). 판을 옮기면 여기도 다시 잰다.
 */
/**
 * **작업대 상판 실물 높이**(user1 z) — `AMR_HOME.topZMm`(−324) 은 여기에 **안전선 +10** 을 더한 그림·게이트용 값이다.
 * 실측 2026-09-04: 거치대2 윗면 짚기 −306.11 − 두께 27.9 = −334.01. 판 위 물건의 **바닥**이 실제로 닿는 높이는 이 값이다 —
 * 색 검출이 화소를 쏘는 평면(윗면 = 여기 + 패드 + 물건 키)도 여기서 시작한다(2026-09-07 · 태그 앵커 z 로 쏘던 것을 바꿈 — 32mm 낮아 트윈이 뚫렸다).
 * ⚠ 판에 캐스터가 있다 — 판을 옮기면 `AMR_HOME.topZMm` 과 같이 다시 잰다.
 */
export const TABLE_TOP_REAL_ZMM = -334.0;
/**
 * 작업대에 깔린 **마우스패드** (실기 담당자 2026-09-07 · 자 — 세로 300 × 가로 775 · 3장). 거치대는 이 위에 놓인다.
 * ⚠ **두께는 아직 안 잤다** — `null` 이면 0 으로 친다(수 mm 급 · 검출 평면·트윈 바닥이 그만큼 낮게 그려진다). 재면 여기 적는다.
 */
export const TABLE_PADS = { count: 3, wMm: 775, dMm: 300, thicknessMm: null, measuredAt: '2026-09-07', source: '실기 담당자 자' };
export const padTopZMm = () => TABLE_TOP_REAL_ZMM + (TABLE_PADS.thicknessMm ?? 0);

export const AMR_HOME = {
  // ── 어느 좌표계의 값인가 ──────────────────────────────────────────────────
  frame: 'user1',               // ⛔ 베이스가 아니다 — `frames.js` 의 `toFrame()` 을 거친다
  // ── 자리 (mm) ────────────────────────────────────────────────────────────
  xMm: 833.1,
  yMm: -948.2,
  // ⭐ **2026-09-04 — 실기 담당자가 판 셋을 올렸다 (+56.9mm).** 거치대2 윗면을 핑거 끝으로 닿게
  // 짚어 `-306.11` 을 읽고 두께 27.9 를 빼 그 판 상판 `-334.01` 을 냈다(검산: 08-27 값에서
  // 같은 두께를 빼면 08-18 에 짚은 `-390.9` 가 정확히 나온다). `config.yaml` 과 **같은 규약**
  // (제일 높은 것 + 여유 10)으로 `-324.0`.
  // ⚠ **잰 것은 거치대2 가 앉은 판(작업대2)이다.** 터틀봇이 달리는 작업대1 은 실기 담당자의
  //    「셋 다 맞췄다」에 기댄다 — 완전한 실측이 아니다.
  // ⚠ 이 값은 원래도 **안전선(+10)** 이라 실물 상판보다 10mm 높다. 그림이 터틀봇을 그만큼
  //    띄워 그린다는 뜻이다 — 옛 값도 같은 성질이었으므로 관계는 안 바뀐다.
  topZMm: -324.0,               // 작업대 상판 — 터틀봇은 바닥이 아니라 판 위를 달린다 (D134)
  // ── 자세 (°) — **모델이 어떻게 구워졌나와 섞지 않는다** ───────────────────
  // `yawDeg` 는 오직 「실물이 어디를 보나」다. 축 규약은 `yawRef` 가 적고,
  // 모델의 정면(+Z)을 이 규약에 맞추는 일은 `view3d/burger.js` 가 한 곳에서 한다.
  // 둘을 한 값에 섞었다가 2026-08-28 에 90° 틀렸다 (작업대2 대신 카트를 봤다).
  yawDeg: 180,                  // 벽을 등지고 정면이 작업대2 쪽 (실기 담당자 확인 · 위 §요각)
  yawRef: '+x 가 0 · 반시계 +', // user1 은 회전 0 이라 베이스와 축이 같다
  // ── 어느 로봇인가 ────────────────────────────────────────────────────────
  // 브리지는 `tb3_1`·`tb3_2` 둘을 등재하지만 **실기는 한 대만 쓴다** (실기 담당자 2026-08-28).
  // 어느 쪽인지는 **추측하지 않고 실행 기록으로 정했다** — `/api/runs` 28건 중
  // **tb3_2 가 27건**, tb3_1 은 19일 전 1건뿐이다. 「지금까지 운행한 그 로봇」이 tb3_2 다.
  // ⚠ 이름표를 바꿔 달면(브링업 네임스페이스 변경) 여기도 같이 고친다 — 화면이 조용히
  //   다른 로봇을 따라간다.
  robotId: 'tb3_2',
  // ── 사람이 실제로 잰 값 (이것이 위 좌표의 출처다) ─────────────────────────
  measured: {
    wallEdgeMm: 255,            // 작업대1 **벽쪽** 세로변(x 1088.1) → 바퀴 센터
    cartEdgeMm: 150,            // 작업대1 **카트쪽** 가로변(y −798.2) → 터틀봇 센터
    by: '줄자 · 실기 담당자', date: '2026-08-28',
  },
  // ── 아직 실측이 아닌 것 (지우기 전에 재야 한다) ───────────────────────────
  assumed: ['yawDeg — 「벽을 등지고 주차」에서 유도. 각도기로 잰 값이 아니다'],
};

/**
 * 터틀봇 윗면 태그 — **글로벌캠이 로봇을 mm 로 재는 열쇠**.
 *
 * 원래 거치대2 조립 지그용(id 18)이었는데, D149 로 거치대2 가 고정 확정돼 태그가 놀았다.
 * 2026-08-28 에 실기 담당자가 **라이다 윗면 정중앙**으로 옮겨 붙였다.
 * 인쇄물은 08-18 것 그대로다 — 실측 한 변 **84.0mm**(배율 0.840 · `tags.json` id18).
 *
 * ⭐ **「정중앙」이 로봇 중심이 아니다.** 라이다가 **바퀴축에서 32mm 뒤**에 앉아 있다 —
 * URDF 배치 그대로다(`scripts/build/burger-glb.py` §URDF 에서 읽은 배치 · `lds` x −0.032).
 * 그래서 오프셋을 `(0,0)` 으로 두면 로봇 자리가 **32mm 뒤로 밀린다.** 이 32 를 빼먹는 것이
 * 오늘 하루 여섯 번 난 그 사고(프레임 라벨·오프셋 누락)와 같은 종류다.
 *
 * ⚠ **아직 소비하는 코드가 없다.** 글로벌캠이 이 태그를 찾아 로봇 자세로 바꾸는 검출기가
 * 아직 0줄이다 — 그래서 `frames.js` 의 `tag` 갈래는 여전히 `unknown` 이다.
 * 검출기가 생기면 그 갈래가 `measured` 가 된다.
 */
export const AMR_TAG = {
  id: 18,
  sizeMm: 84.0,                 // 실측 한 변 (선언 100 × 인쇄배율 0.840) — `tags.json` 정본
  onRobot: '라이다 윗면 정중앙',
  facing: 'up',                 // **눕힘** — 글로벌캠이 위에서 본다 (다른 태그는 세움)
  // 로봇 프레임(ROS · +x 앞) 에서 본 태그 중심
  offsetMm: { x: -32, y: 0, z: 192 },
  _offsetMm: 'x −32 는 라이다가 바퀴축 뒤에 앉은 거리(URDF) · z 192 는 버거 전체 높이 = 라이다 윗면',
  measuredAt: '2026-08-28',
};

/**
 * 그리퍼 손가락 실물 — **바구니에 같이 들어가야 하는 물건**이다 (2026-08-31).
 *
 * ⭐ **단위가 닫혔다 (2026-09-03 · 실기 담당자 「1.3cm 임」).** 08-31 에 「1.5」만 듣고 cm·mm 를
 * 못 가려 `15` 를 보수적으로 넣어 뒀는데, **값 자체가 1.5 가 아니라 1.3 이었다.**
 * 단위만 물었으면 `15` 로 굳었을 자리다 — 되물을 때는 **숫자와 단위를 같이** 확인한다.
 *
 * 바뀌는 것 — 바구니 안깊이 120 에 거치대 85 를 넣고 손가락 둘이 같이 들어가면
 * 남는 세로 여유가 `120 − 85 − 2×13 = 9mm`(전에 적힌 5mm 는 15 기준이었다).
 * ⚠ 여전히 **한쪽 4.5mm** 다. 손가락째 넣는 방식은 정합이 그만큼 정확해야 성립한다 —
 * 오늘 잰 사슬 오차가 100mm 대이므로 **지금은 「위에서 놓기」만 성립한다.**
 */
export const GRIPPER_FINGER = {
  thicknessMm: 13,            // 실기 담당자 「1.3cm」 (2026-09-03) — 단위·값 둘 다 확인됨
  thicknessSrc: '실기 담당자 구두 · 1.3cm 확인 (2026-09-03). 08-31 의 「1.5」는 잘못 들은 것',
  widthMm: null,              // ← 미측정
  lengthMm: null,             // ← 미측정
};

/**
 * **시나리오 ①(pile)의 정차 자리** — 팔이 거치대1 에서 집어 터틀봇에 싣는 동안 서 있는 곳
 * (2026-08-31 · D163). 시나리오 정본은 `Shared/data/scenario/presets.js` 의 `ASSEMBLY_AMR` 이다.
 *
 * ## 어떻게 골랐나 (지어낸 값 0)
 *
 * `scripts/dev/amr-stop-mujoco.mjs` — 직선 위 자리 × 요각 4 를 훑어, 자리마다 **순서 6칸
 * 전부**(접근·문다·든다·옮긴다·테두리·넣는다)를 `POST /ik` 로 풀고 **무조코로 실제 충돌**을 봤다.
 * 328회 질의 · 통과 **28자리**.
 *
 * ⭐ **집는 것까지 넣자 답이 절반으로 줄었다** — 넣는 자세만 보면 59자리였는데, 팔의
 * **집는 길**을 터틀봇이 막는 경우가 걸러져 28자리가 됐다. 요각 0°·90° 는 통째로 탈락했다.
 *
 * ## ⛔ 직선만 간다
 *
 * 차동구동이라 **자기 heading 앞뒤**로만 간다. 홈이 곧 `odom` 원점이므로 직진하면
 * `odom y` 가 0 에 머물고, `user1` 로 옮기면 **y 가 −948.2 로 고정**된다(실측 검산 2026-08-31).
 * ⛔ 대각선 자리를 추천했던 초안은 폐기했다 — 터틀봇이 못 가는 곳이었다.
 *
 * ## ⛔ **회전은 안 한다 — 앞으로만 간다** (실기 담당자 2026-08-31 정정)
 *
 * 한때 요각 270° 를 골랐다(팔이 187mm 덜 뻗는다). 그런데 그러려면 **제자리 90° 회전**이
 * 필요하고, 회전은 우리 오차의 주범이다(D141 · 직진은 600mm 에 0.4°, 오차는 전부 회전에서).
 * 바구니가 로봇 중심에서 135mm 뒤라 **요각 1° = 바구니 2.4mm** 밀림이다.
 *
 * ⭐ **앞으로 가면 회전 없이 같은 것을 얻는다.** 직진 훑기(span 700 · 요각 180 고정):
 *
 *     직진   0mm  팔 1353mm · 정차 25.8초   (홈)
 *     직진 100mm  팔 1284mm · 정차 23.9초
 *     직진 200mm  팔 1218mm · 정차 **21.7초**   ← 채택 (정차 최소)
 *     직진 300mm  팔 1158mm · 정차 25.0초
 *     직진 700mm  팔  985mm · (되는 끝)
 *
 * **200mm 에서 정차 시간이 최소다** — 더 가면 팔은 덜 뻗지만 관절이 많이 돌아 오히려 느리다.
 * 270°(38.4초) 보다 **16.7초 빠르고 회전 오차도 0** 이다 — 모든 면에서 낫다.
 */
export const AMR_DROP = {
  frame: 'user1',
  station: 'pile',            // `ASSEMBLY_AMR` 의 ① — 팔이 싣는 자리
  xMm: 633.1,                 // 홈(833.1)에서 **앞으로 200mm**. 되는 구간은 −200~+700mm
  yMm: -948.2,                // ⛔ 안 변한다 — 직진하면 `odom y` 가 0 에 머문다(실측 검산)
  yawDeg: 180,                // = 홈 요각. **회전 0** — 위 §회전은 안 한다
  fromHomeMm: 200,            // odom (200, 0)
  turnFromHomeDeg: 0,         // ⭐ 제자리 회전 없음 — 회전 오차가 0 이다
  /**
   * 팔이 싣는 동안 터틀봇이 서 있어야 할 시간. 관절 이동량 ÷ 2.89°/s(상한 10%).
   * ⚠ **하한이다** — 가감속·정착 대기를 안 넣었다. 실주행에서 더 걸린다.
   * ⛔ 시나리오의 `tSec` 0~12초는 **임시값**이라고 정본이 스스로 적어 뒀다 — 이 값이 그 자리를 채운다.
   */
  dwellSec: 21.7,
  source: 'mujoco-sweep',
  assumesBasketOffset: true,  // ← `AMR_BASKET.offsetMm` 을 재면 false 가 되고 값이 갱신된다
  measuredAt: '2026-08-31',
};

export const AMR_DROP_CANDIDATES = [
  { xMm: 833.1, yMm: -948.2, yawDeg: 270, fromHomeMm: 0, turnFromHomeDeg: 90, reachMm: 1178, dwellSec: 38.8, note: '팔이 제일 덜 뻗는 축 · 지금 주차한 자리 그대로' },
  { xMm: 833.1, yMm: -948.2, yawDeg: 180, fromHomeMm: 0, turnFromHomeDeg: 0, reachMm: 1341, dwellSec: 25.5, note: '회전 0 · 지금 자리 그대로 · 제일 빠르다' },
  { xMm: 783.1, yMm: -948.2, yawDeg: 270, fromHomeMm: 50, turnFromHomeDeg: 90, reachMm: 1143, dwellSec: 40.7, note: '가장 짧은 뻗음' },
  { xMm: 883.1, yMm: -948.2, yawDeg: 270, fromHomeMm: -50, turnFromHomeDeg: 90, reachMm: 1214, dwellSec: 37.1, note: '뒤로 50 · 270°' },
  { xMm: 783.1, yMm: -948.2, yawDeg: 180, fromHomeMm: 50, turnFromHomeDeg: 0, reachMm: 1306, dwellSec: 24.6, note: '최단 정차 시간' },
  { xMm: 883.1, yMm: -948.2, yawDeg: 180, fromHomeMm: -50, turnFromHomeDeg: 0, reachMm: 1377, dwellSec: 26.3, note: '뒤로 50 · 회전 0' },
];

/**
 * **정차 자리 후보** — 위 `AMR_DROP` 하나로는 «왜 그 자리인가» 를 사람이 못 따진다 (2026-09-03).
 *
 * 08-31 은 요각 180° 만 훑어 **한 자리**를 냈다. 범위를 넓히니 **270° 라는 다른 축**이 나왔고,
 * 둘은 우열이 아니라 **맞바꿈**이다 — 270° 는 팔이 덜 뻗고(여유가 커진다), 180° 는 빨리 끝난다.
 * 그래서 하나를 고르지 않고 **후보를 늘어놓고 사람이 고르게** 한다. 화면(시뮬 탭)이 이걸 읽는다.
 *
 * ## ⭐ 2026-09-04 에 통째로 다시 뽑았다 — **한 번에 한 축만 바꾼다**
 *   `node scripts/dev/view-mujoco.mjs --target 504.7,-1217.6,-330.0 --azis 135 --dists 300 --json`
 *
 *   옛 표는 「닿는 자리 상위 6개」였는데, 오늘 훑어 보니 **120자리 중 97개가 통과**한다.
 *   즉 「닿나」에는 더 이상 정보가 없다 — 아무 데나 닿는다. **남은 미지는 시선각 하나**다.
 *   그래서 **방위 135°·거리 300mm 를 고정하고 시선각만 0·10·20·30·40°** 로 갈랐다.
 *   실물로 재면 「몇 도에서 제일 잘 보이나」가 **한 판에 갈린다** — 두 축을 같이 바꾸면
 *   결과가 나와도 어느 쪽 덕인지 못 가른다.
 *
 *   왜 방위 135° 인가 — 팔이 그쪽에서 접근하면 덜 뻗는다(647~717mm · 오늘도 상위를 쓸었다).
 *   왜 거리 300mm 인가 — 카메라가 표적(판 위 태그)에서 300 이면 **거치대 윗면에서는 245mm**
 *   다(거치대가 55mm 높다). 250 을 고르면 윗면이 **195mm** 로 D435 사각지대에 걸린다(D166).
 *
 * ## 시선각의 뜻 — 0°가 되는 자리가 많다고 좋은 게 아니다
 *   0°(바로 위)  세운 총알이 **자기를 스테레오 그림자로 지운다** (무효 21% · 8개 중 4개만)
 *   88.4°(옆)    평면이 깨져 덩어리 **0개** (2026-08-13)
 *   `VISION-SERVO-LADDER` §7 앞에 선 물리 벽이 그것이고, 실용 상한을 **40°** 로 적어 뒀다.
 *
 * ⛔ **무조코는 「보이나」를 모른다.** 여기 다섯은 **갈 수 있고 안 부딪히는** 자리일 뿐이다 —
 *   보이는지는 `scripts/robot/carrier-find.py` 가 그 자리에서 재야 답이 나온다.
 * ⚠ 표적이 옮겨지면 이 표는 통째로 낡는다 — 훑기를 다시 돌린다(그게 스크립트인 이유).
 */
export const VIEW_CANDIDATES = [
  { tiltDeg: 0, aziDeg: 135, distMm: 300, reachMm: 717,
    tcpMmDeg: [580.8, -1191.7, -111.7, 180, 0, 90],
    jointsDeg: [96.29, -59.49, 67.15, -97.67, -90, 96.29] },
  { tiltDeg: 10, aziDeg: 135, distMm: 300, reachMm: 684,
    tcpMmDeg: [553.4, -1163.9, -108.9, 172.9, 7.1, 90],
    jointsDeg: [91.87, -68.4, 82.4, -111.33, -83.18, 91.86] },
  { tiltDeg: 20, aziDeg: 135, distMm: 300, reachMm: 710,
    tcpMmDeg: [520.8, -1197.1, -98.8, 160, 0, 45],
    jointsDeg: [86.44, -68.22, 83.85, -119.18, -75.14, 133.21] },
  { tiltDeg: 30, aziDeg: 135, distMm: 300, reachMm: 677,
    tcpMmDeg: [492.4, -1168.7, -102.9, 150, 0, 45],
    jointsDeg: [80.75, -74.01, 96.4, -131.03, -66.06, 129.74] },
  { tiltDeg: 40, aziDeg: 135, distMm: 300, reachMm: 647,
    tcpMmDeg: [465, -1141.3, -113.9, 140, 0, 45],
    jointsDeg: [74.44, -77.45, 107.46, -142.42, -55.96, 126.38] },
];

/** 위 후보를 낼 때 쓴 표적 (user1 mm) — **2026-09-04 글로벌캠이 본 태그 15** (errPx 0.04).
 *  ⛔ 정본이 아니다. 거치대를 옮기면 이 값과 위 표가 같이 낡는다. */
export const VIEW_TARGET_MM = [504.7, -1217.6, -330.0];

/** 손목 뎁스카메라 — 렌즈가 TCP 위 몇 mm 인가. 정본은 `docs/ref/arch/DEPTH-CAM.md`. */
export const DEPTH_LENS_ABOVE_TCP_MM = 80;
/**
 * 손목 뎁스(D435 · 848×480)가 **쓸 만한 깊이 구간** — 시야 발자국의 초록/회색 판정에 쓴다 (2026-09-06 · `GRILL-conveyor-twin` #11).
 * `minZMm` 195 는 데이터시트 Table 4-11 이고 08-05 높이 스윕이 「여기부터 쓸 만하다」로 검증했다(`DEPTH-CAM.md` §Min-Z).
 * `farMm` 1000 은 **결정값**이다 — 08-08 스윕이 670mm 높이에서 112~868mm 를 봤고 그 근처 상한을 잡았다. 실측 상한이 아니다.
 */
export const DEPTH_USEFUL_MM = { minZMm: 195, farMm: 1000 };   // 실기 접촉값을 2.7mm 안에서 재현

/**
 * `speedPct 100` 일 때의 관절 각속도. **벤더 사양(180°/s)이 아니다** — `MoveJ` 의 `vel`
 * 백분율은 관절 기계적 최대의 백분율이 아니다. 실측이 6배 넘게 느리다.
 *
 * 재는 법: Δ 1·2·4·5° 를 각 2회, 속도 10·5·3% 로 j1 을 움직여 도착까지 재고 회귀
 * (2026-08-08 · `docs/evidence/2026-08-08/fr5-motion-timing-measured.md`). 비례도 확인됐다.
 * 정본은 `FR5/bridge/commands.py` 의 `JOINT_DEG_S_AT_FULL` 이다 — 브리지가 실제로 쓰는 값이라.
 * ⚠ 펜던트의 전역 속도 오버라이드가 바뀌면 이 값도 바뀐다. 그러면 다시 잰다.
 *
 * ⛔ **그 경고가 2026-08-12 에 현실이 됐다.** 오버라이드가 **0%** 로 내려가 있어 외부 `MoveJ` 가
 * `code=172`("Motion Speed Cannot Be 0")로 **18/18 거부**됐다. 사람이 **30%** 로 올려 해소.
 * 그런데 **08-08 에 이 28.9 를 잴 때의 오버라이드 값은 기록이 없다** — 같은 값인지 모른다.
 * 즉 **지금 이 상수는 검증되지 않은 상태다.** 다시 재기 전에는 대기가 짧을 수 있고, 짧으면
 * 응답이 「도착」인데 로봇은 가는 중이라 다음 명령이 겹친다(D89 가 막으려던 사고).
 * 재는 법은 08-08 과 같다 — Δ 1·2·4·5° × 속도 10·5·3% 회귀 (`evidence/2026-08-08/`).
 */
export const JOINT_DEG_S_AT_FULL = 28.9;
/** 이동 하나의 고정 비용 — 가감속 램프 + 지령 왕복. 회귀 절편 0.315s */
export const MOVE_FIXED_S = 0.315;

/**
 * 터틀봇 직진 속도 — **실측** (2026-09-05 순찰 기록 `TurtleBot/bridge/data/runs/2026-09-05-211406-tb3_1-example_patrol.path.json`
 * · 1초 표본 두 점 사이 138.5mm). 스펙 최고속 0.22m/s 가 아니다 — 우리 경로 실행이 실제로 낸 속도다.
 * 컨베이어 시연(`Shared/data/sim/cycle.js`)의 주행 시간이 이 값으로 나온다 (`GRILL-conveyor-twin` #7).
 */
export const AMR_DRIVE_MM_S = 138.5;
/**
 * 컨베이어 시연에서 터틀봇이 정차 자리에서 **앞으로** 갔다 되돌아오는 거리 (`GRILL-conveyor-twin` #4).
 * 홈 기준 +700mm 가 되어 「되는 구간 −200~+700」(`AMR_DROP.xMm` 주석)의 끝이다 — 더 늘리지 않는다.
 */
export const AMR_SHUTTLE_MM = 500;
/**
 * 터틀봇 **도착 오차** — 경로 실행이 웨이포인트에 닿을 때 35~40mm 못 미친다 (2026-08-20 실측 4점 전부 ·
 * `docs/evidence/2026-08-20/tb-drift-turn.md`). 직진 자체는 842mm 에 0.5mm 라 완벽하고, 회전이 매번 2~11° 부족한 것이
 * 범인이다. 시연의 드리프트 유령(`cycle.js` `driftPose`)은 이 값을 **주행 방향으로 짧게** 넣는다. 회전 경로는 아직 안 그린다.
 */
export const AMR_ARRIVE_ERR_MM = 37.5;
/**
 * 터틀봇 **회전 부족** — 경로 실행이 회전을 매번 덜 돈다: 6.7 · 1.9 · 10.7 · 6.9° (같은 08-20 실측 4회전 · 평균 6.6°).
 * 각 점에서 위치를 다시 맞춰 누적은 안 되지만, 회전 직후 그만큼 어긋난 방향으로 출발한다. 시연 유령은 제자리 회전이 있는
 * 정차 후보(`turnFromHomeDeg` 90)에서 요각을 이만큼 어긋나게 그린다 — 그 방향 오차가 다음 직진에서 자리로 번지는 모양은 실측이 없어 안 그린다.
 */
export const AMR_TURN_DEFICIT_DEG = 6.6;
/**
 * 터틀봇 홈 **두 번째 후보** — 손목 뎁스로 라이다 윗면을 잰 (odom, user1) 쌍 3개(`scripts/robot/amr-pairs.json` · `amr-pair.py`)를
 * 요각 180 고정으로 풀면 홈 = user1 − R(180)·odom = (724.8 − 32, −931.3). 정본 `AMR_HOME`(줄자 · 833.1, −948.2)과 **141mm** 차 —
 * GAP 의 「AMR_HOME 140mm」 P0 가 이것이다. 잔차: 쌍 2 는 5.7mm · 쌍 3 은 14.8mm(y · 요각 0.1° 급).
 * ⛔ 정본을 바꾸지 않는다 — 실측 쌍 6개가 오면 어느 쪽인지 판정한다 (GAP 6). 화면은 둘 다 그린다(`GRILL-conveyor-twin` #14).
 * `Shared/data/sim/amr-home-alt.test.js` 가 이 숫자를 쌍 파일에서 다시 풀어 지킨다.
 */
export const AMR_HOME_ALT = {
  frame: 'user1', xMm: 692.8, yMm: -931.3, yawDeg: 180,
  source: 'scripts/robot/amr-pairs.json — 손목 뎁스 라이다 평면 · 쌍 3개', measuredAt: '2026-09-03',
};

/** 사람이 읽는 요약. 화면·리포트가 이걸 그대로 쓴다 — 값이 늘면 여기도 늘린다. */
export const MEASUREMENTS = [
  src(`카트 상판 ${CART.wMm} × ${CART.dMm} mm`,
    '줄자 재측정 — 옛 810×597 과 2·1mm 차 · 런북의 858×580 이 틀렸다', '2026-08-08'),
  src(`카트 높이 ${CART.heightMm} mm (바닥→데크)`, '줄자 985 + 15'),
  src(`마운트판 ${MOUNT_PLATE.sideMm} × ${MOUNT_PLATE.sideMm} mm`, '줄자'),
  src(`마운트판 두께 ${MOUNT_PLATE.thickMm} mm`, '뎁스 2점 차'),
  src('로봇 장착면 = 베이스 원점 (±7mm)', '뎁스 + user1 환산'),
  src('user1 = (−401.8, +497.3, +342.1) · 회전 0', 'GetWObjCoordWithID(1) readback'),
  src('활성 툴 = [0, 0, 135] — TCP 는 손끝이다', 'GetCurToolCoord readback'),
  src(`터틀봇 태그 id${AMR_TAG.id} = 라이다 윗면 · 중심 (${AMR_TAG.offsetMm.x}, ${AMR_TAG.offsetMm.y}, ${AMR_TAG.offsetMm.z})`,
    '한 변 84.0 실측(인쇄배율 0.840) · 오프셋은 URDF 의 라이다 배치', '2026-08-28'),
  src(`터틀봇 홈 = (${AMR_HOME.xMm}, ${AMR_HOME.yMm}) · 상판 ${AMR_HOME.topZMm} · **user1 기준**`,
    '줄자 — 작업대1 오른쪽 변 255 · 카트 방향 가로변 150 (판은 로봇이 짚은 실측)', '2026-08-28'),
  // ⛔ **읽을 API 가 없다** — SDK 에 setter(`SetSpeed`)만 있고 getter 도 구조체 필드도 없다.
  // 그래서 이 값은 **사람이 펜던트에서 본 것**이고, 우리 화면은 지금 이걸 못 보여준다.
  // 0% 였던 것이 08-12 에 외부 이동을 통째로 막았는데 화면엔 숫자만 떴다 (`code=172`).
  src('전역 속도 오버라이드 = 30% — 08-12 에 0% 였던 것을 사람이 올렸다',
    '펜던트 화면 · 사람이 읽고 설정 (읽는 API 가 없다)', '2026-08-12'),
  src(`뎁스 렌즈 = TCP 위 ${DEPTH_LENS_ABOVE_TCP_MM} mm`, '실기 접촉을 2.7mm 안에서 재현'),
  src(`관절 각속도 = ${JOINT_DEG_S_AT_FULL} °/s (speedPct 100 환산)`,
    'j1 로 Δ·속도 스윕 회귀 · 벤더 사양 180 아님', '2026-08-08'),
  src(`이동 고정비용 ${MOVE_FIXED_S} s (가감속 램프)`, '같은 회귀의 절편', '2026-08-08'),
  src(`터틀봇 직진 ${AMR_DRIVE_MM_S} mm/s`, '09-05 순찰 기록 1초 표본 두 점 · 스펙 0.22m/s 아님', '2026-09-05'),
  src('뎁스 광축 롤 = 0° (카메라 X축 = 툴 X축)', '벽 평면 + 화각 일관성', '2026-08-08'),
  src('바닥 = 카트 상판 −1000mm', '뎁스 실측 −1346.3 vs 계산 −1345.8 · **0.5mm**', '2026-08-08'),
  // 값을 안 적는다 — 정본이 게이트 상자라 여기 베끼면 다시 재는 날 갈라진다 (§SIDE_STAND)
  src('거치대 상판 = 게이트 상자 `작업대` (가로는 줄자·세로높이는 뎁스)',
    '손목 뎁스 1534점 · 잔차 2.9mm · 줄자 대조 2.3% · config.yaml', '2026-08-08'),
  src(`⚠ 거치대 상판 밑(선반 ${SIDE_STAND.shelfHMm} · 파이프 ${SIDE_STAND.tubeMm}) — **관찰값**`,
    '영상 관찰 · 판정에 안 쓰인다', '2026-08-08'),
];
